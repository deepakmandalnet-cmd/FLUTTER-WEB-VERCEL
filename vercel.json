{
  "installCommand": "echo 'Cloning Flutter...' && git clone https://github.com/flutter/flutter.git --depth 1 -b stable /flutter && echo 'Running flutter doctor...' && /flutter/bin/flutter doctor && echo 'Getting dependencies...' && /flutter/bin/flutter pub get && echo 'Building Flutter web app...' && /flutter/bin/flutter build web --release",
  "buildCommand": "echo 'Build complete.'",
  "outputDirectory": "build/web",
  "rewrites": [
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}
