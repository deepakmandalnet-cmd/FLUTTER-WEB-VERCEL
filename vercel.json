{
  "installCommand": "chmod +x setup.sh && ./setup.sh",
  "buildCommand": "echo 'Build artifacts were created during the install phase.'",
  "outputDirectory": "build/web",
  "rewrites": [
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}
