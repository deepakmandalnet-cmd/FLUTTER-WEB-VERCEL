{
  "builds": [
    {
      "src": "pubspec.yaml",
      "use": "@vercel/static-build",
      "config": {
        "installCommand": "git clone https://github.com/flutter/flutter.git --depth 1 --branch stable /tmp/flutter && /tmp/flutter/bin/flutter pub get",
        "buildCommand": "/tmp/flutter/bin/flutter build web --release",
        "outputDirectory": "build/web"
      }
    }
  ],
  "rewrites": [
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}